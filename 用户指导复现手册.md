# 用户指导复现手册

本文档面向首次复现实验的读者，目标是在一台 Linux GPU 机器（推荐 AutoDL/云主机）上，从零开始跑通：数据准备 → 目标/影子模型训练 → 特征提取（统计 + AST + Embedding + LiRA）→ 攻击评估与图表导出。

## 1. 运行环境

- 操作系统：Linux（AutoDL 容器/Ubuntu 22.04 等）
- Python：建议 3.10+（AutoDL 上常见 3.10/3.11/3.12 均可）
- GPU：建议 16GB+ 显存（本项目在 24GB 上可顺畅运行）

补充建议：

- 磁盘：建议预留 30GB+（数据缓存 + 多个模型权重 + 图表输出）
- 网络：需要下载数据集与 HuggingFace 模型；脚本默认使用 `HF_ENDPOINT=https://hf-mirror.com`

## 2. 目录结构与关键文件

建议进入目录：`AutoDL_MIA/`

- `prepare_mia_data.py`：下载 Concode 并严格划分 Target/Shadow 数据集
- `train_target_model.py`：训练目标模型（Causal LM）
- `train_shadow_models.py`：训练多个影子模型（Causal LM）
- `extract_features.py`：提取攻击特征（统计特征 + AST + Embedding + LiRA 差异特征）
- `mia_pipeline/cli.py`：攻击评估入口（classic / fes），生成指标表与图表；少样本模式可输出 Log-MIA 报告
- `run_all.sh`：一键流水线脚本（支持跳过阶段）
- `requirements.txt`：依赖列表

运行产物：

- `mia_data/`、`mia_data_raw/`：数据集缓存
- `models/target_model/`、`models/shadow_model_*/`：训练好的模型
- `attack_train_dataset.csv`、`attack_test_dataset.csv`：攻击训练/测试特征表
- `plots/main_models/`、`plots/ablation/`：论文图表

## 3. 一键复现（推荐）

在 `AutoDL_MIA/` 目录下：

```bash
bash run_all.sh
```

`run_all.sh` 默认会依次执行：安装依赖 → 准备数据 → 训练目标 → 训练影子 → 提特征 → 攻击评估并画图。

少样本模式（资源受限 / 快速跑通链路）：

```bash
MIA_MODE=fes bash run_all.sh
```

说明：当 `NUM_SHADOW_MODELS=1` 时，LiRA 相关特征会退化（参考模型为空），本实现会把 LiRA 差异特征置为 0 以避免 NaN；若希望 LiRA 有效，建议设置 `NUM_SHADOW_MODELS>=2`。

## 4. 分步复现（便于排错）

### 4.1 安装依赖

```bash
pip install -r requirements.txt
```

### 4.2 准备数据

```bash
python prepare_mia_data.py
```

预期生成：`mia_data/` 与 `mia_data_raw/`。

### 4.3 训练目标模型

```bash
python train_target_model.py
```

预期生成：`models/target_model/`。

### 4.4 训练影子模型

```bash
python train_shadow_models.py
```

预期生成：`models/shadow_model_0/` … `models/shadow_model_4/`。

### 4.5 提取攻击特征

```bash
python extract_features.py
```

预期生成：

- `attack_train_dataset.csv`
- `attack_test_dataset.csv`

### 4.6 训练攻击模型并生成图表

```bash
python -m mia_pipeline.cli \
  --mode classic \
  --models rf,xgb,thr_loss,thr_lira_z \
  --output-dir plots/main_models \
  --ignore-weak-ast
```

预期生成：

- `plots/main_models/`：主实验图表

补充：

- 若你运行 `defense_sweep.py`（可选），会在 `defense_runs/<defense_name>/plots/<scenario>/` 下生成多组对比图表与指标表

少样本模式对应命令（会输出 Log-MIA 报告）：

```bash
python -m mia_pipeline.cli \
  --mode fes \
  --shots-per-class 64 \
  --models lr,thr_loss,rf \
  --output-dir plots/main_models \
  --ignore-weak-ast \
  --logmia
```

## 5. run_all.sh 跳过阶段（节省时间）

`run_all.sh` 支持用环境变量跳过阶段（值为 `1` 表示跳过）：

- `SKIP_INSTALL`：跳过 `pip install`
- `SKIP_DATA`：跳过数据准备
- `SKIP_TRAIN_TARGET`：跳过目标模型训练
- `SKIP_TRAIN_SHADOW`：跳过影子模型训练
- `SKIP_EXTRACT`：跳过特征提取
- `SKIP_ATTACK`：跳过攻击训练与画图

攻击阶段常用可选参数（不跳过时生效）：

- `ATTACK_OUTPUT_DIR`：图表输出目录（默认 `plots/main_models`）
- `ATTACK_MODELS`：要评估的攻击器/基线列表（逗号分隔）
  - classic 默认 `rf,xgb,thr_loss,thr_lira_z`
  - fes 默认 `lr,thr_loss,rf`
- `FEWSHOT_SHOTS_PER_CLASS`：少样本每类样本数（`MIA_MODE=fes` 时默认 `64`）

示例：模型已训练完，仅重跑“提特征 + 攻击评估”：

```bash
SKIP_INSTALL=1 SKIP_DATA=1 SKIP_TRAIN_TARGET=1 SKIP_TRAIN_SHADOW=1 bash run_all.sh
```

示例：特征 CSV 已存在，仅重跑攻击评估：

```bash
SKIP_INSTALL=1 SKIP_DATA=1 SKIP_TRAIN_TARGET=1 SKIP_TRAIN_SHADOW=1 SKIP_EXTRACT=1 bash run_all.sh
```

## 6. 论文图表位置建议（对应第5章）

- 主实验：`plots/main_models/roc_curve_log.png`、`plots/main_models/pr_curve.png`、`plots/main_models/tpr_bar_chart.png`
- 阈值优化与误报控制：`plots/main_models/threshold_curve_*.png`、`plots/main_models/confusion_*_{default_0.5|tuned}.png`
- 低 FPR 区间权衡：`plots/main_models/det_curve_loglog.png`

可选（防御/可见性对比图表）：

- `defense_runs/<defense_name>/plots/<scenario>/roc_curve_log.png`
- `defense_runs/<defense_name>/plots/<scenario>/metrics_table.csv`

## 7. 常见问题

### 7.1 `libgomp: Invalid value for environment variable OMP_NUM_THREADS`

训练脚本已显式设置线程变量；若仍出现，确保没有在外部设置异常值（如空字符串）。

### 7.2 `tree-sitter` 报错 `Language.__init__() missing ... 'name'`

本项目在 `extract_features.py` 中做了兼容处理（不同 `tree-sitter` 版本的 `Language(...)` / `Parser` API 可能略有差异）。若仍报错，优先锁定 `tree-sitter==0.21.x` 并确保已安装 `tree_sitter_java`。

### 7.3 `RuntimeError: xgboost is not installed but requested`

安装 `xgboost`，或在攻击阶段把 `--models` / `ATTACK_MODELS` 中的 `xgb` 去掉（例如仅用 `rf,thr_loss`）。

### 7.4 训练耗时过长

可先跑通攻击链路：

- 只跑 `extract_features.py` + `python -m mia_pipeline.cli ...`（前提是 `models/` 已存在）
- 或临时减少影子模型数量/训练 epoch（用于调试，不建议用于最终论文结果）
